services:
  - type: web
    name: baseball-2025-tracker
    runtime: python
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn app:server --bind 0.0.0.0:$PORT --timeout 30
    envVars:
      - key: PYTHON_VERSION
        value: "3.12"
